import os

# Ejecutar el comando del sistema para crear un directorio llamado "my_first_directory"
returned_value = os.system("mkdir my_first_directory")

# Imprimir el valor retornado por el comando del sistema
print(returned_value)
